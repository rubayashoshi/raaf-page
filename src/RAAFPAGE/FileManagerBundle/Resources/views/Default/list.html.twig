{% for document in documents %}
    <img src="{{ asset(document.path ~ '/' ~ document.name) }}"><br/>
{% endfor %}

<a href="{{ path('RAAFPAGE_file_manager_upload_document') }}">Upload a new document</a>